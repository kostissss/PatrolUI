import { FormsDirectiveDirective } from './forms-directive.directive';

describe('FormsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FormsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
